# cv-export

Node.js helper tool for generating CV exports from structured data.

## Files
- `cv-data.js`: input CV payload
- `generate-cv.js`: export generator entrypoint
- `preprocess_photo.py`: optional image preprocessing helper

## Usage
```bash
npm install
npm run build
```

The generated output is intended for local tooling workflows and does not replace the API export endpoints.

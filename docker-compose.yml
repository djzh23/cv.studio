services:
  postgres:
    image: postgres:16
    container_name: cvstudio-postgres
    environment:
      POSTGRES_DB: cvstudio
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - cvstudio-postgres-data:/var/lib/postgresql/data

volumes:
  cvstudio-postgres-data:
